import numpy as np
import pandas as pd
np.random.randint(low=1, high=100, size=12)
a = np.random.randint(low=0, high=2, size=100)
mylist = list(a)
#now from we converted an array to a list so it can be pasted into excel